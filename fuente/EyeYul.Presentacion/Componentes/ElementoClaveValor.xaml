<UserControl x:Class="EyeYul.Presentacion.Componentes.ElementoClaveValor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="150">
    <StackPanel Margin="0,0,20,10">
        <TextBlock Text="{Binding Clave, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   FontFamily="{StaticResource MonoFont}" 
                   FontSize="10" 
                   FontWeight="Bold" 
                   Foreground="{DynamicResource MutedBrush}" 
                   Margin="0,0,0,4" />
        <TextBlock Text="{Binding Valor, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="{DynamicResource InkBrush}" />
    </StackPanel>
</UserControl>

