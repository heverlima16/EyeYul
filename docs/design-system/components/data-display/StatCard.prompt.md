Compact metric tile — icon chip + uppercase mono label + bold display-font value. Used for streaks, totals, percentages.

```jsx
<StatCard icon={<Flame size={18}/>} label="Racha de Días" value="4 Días" tone="orange" />
```
