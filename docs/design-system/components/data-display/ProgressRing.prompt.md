Circular countdown ring — the big dashboard timer ("Próxima Pausa") and the break-overlay breathing/countdown ring.

```jsx
<ProgressRing percent={62} centerLabel="14:32" subLabel="Próxima Pausa" />
```

Track is a flat neutral circle; progress arc is terracotta (or amber when paused). Center label is always mono/bold.
