Sidebar navigation row used in the app's left rail (Vista General, Historial, Postura y Parpadeo, Smart Pause, Frecuencia/Agenda/Límites/Frases).

```jsx
<SidebarNavItem icon={<Activity size={16}/>} label="Vista General" active onClick={()=>setTab('dashboard')} />
```

Group related items under a small uppercase mono section header (e.g. "MONITOR E HISTORIAL") — see the Dashboard UI kit screen.
